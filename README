# This is flihabi kernel iso

Use git config:
                git config core.compression 0
                git config core.loosecompression 0
                pack.window 0
for proper use of git with an iso file
